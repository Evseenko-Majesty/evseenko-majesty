// ============================================
// ЗАГРУЗОЧНЫЙ ЭКРАН — ЛОГИКА
// Пока только показывает экран
// ============================================

import { render } from './view.js';

export class SplashScreen {
  constructor(app) {
    this.app = app;  // Ссылка на главное приложение (чтобы управлять переходами)
  }
  
  // Возвращает HTML-элемент экрана
  getElement() {
    return render();
  }
  
  // Вызывается после показа экрана
  onMount() {
    // Пока пусто. Потом: анимация, проверка сервера, переход на главную
  }
}
