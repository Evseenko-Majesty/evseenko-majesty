// ============================================
// ГЛАВНЫЙ ЭКРАН — ПРЕДСТАВЛЕНИЕ
// Пока пустой, будет наполняться
// ============================================

export function render() {
  const div = document.createElement('div');
  div.className = 'home';
  
  // Временно пусто
  // Позже добавим: профиль, карточки услуг, историю
  
  return div;
}
