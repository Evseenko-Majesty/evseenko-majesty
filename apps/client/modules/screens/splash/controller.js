// ============================================
// ЗАГРУЗОЧНЫЙ ЭКРАН — ЛОГИКА
// ============================================

import { render } from './view.js';

export class SplashScreen {
  constructor(app) {
    this.app = app;  // Ссылка на главное приложение
  }
  
  // Отдаёт HTML-элемент экрана
  getElement() {
    return render();
  }
  
  // Вызывается после того как экран показан
  onMount() {
    // Пока пусто
    // Потом: анимация, подключение к серверу, переход дальше
  }
}