// ============================================
// ЗАГРУЗОЧНЫЙ ЭКРАН — ЛОГИКА
// ============================================

import { render } from './view.js';

export class SplashScreen {
  constructor(app) {
    this.app = app;
  }
  
  // Возвращает HTML-элемент экрана
  getElement() {
    return render();
  }
  
  // Вызывается после отрисовки
  onMount() {
    // Пока пусто
    // Потом: анимация, подключение к серверу, переход на главную
  }
}
