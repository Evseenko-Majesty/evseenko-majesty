{
  "name": "evseenko-majesty-server",  // Название проекта
  "version": "1.0.0",
  "type": "module",                    // Можно использовать import/export
  "scripts": {
    "start": "node index.js"           // Команда запуска (npm start)
  },
  "dependencies": {
    "express": "..." ,                 // Создаёт сервер, обрабатывает запросы
    "@supabase/supabase-js": "...",    // Подключение к Supabase (база данных)
    "cors": "...",                     // Разрешает запросы с других доменов
    "dotenv": "..."                    // Читает секретные ключи из .env
  }
}
